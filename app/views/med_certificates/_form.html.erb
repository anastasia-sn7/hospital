<%= form_with(model: med_certificate) do |form| %>
  <% if med_certificate.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(med_certificate.errors.count, "error") %> prohibited this med_certificate from being saved:</h2>

      <ul>
        <% med_certificate.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :doctor_id, style: "display: block" %>
    <%= form.text_field :doctor_id %>
  </div>

  <div>
    <%= form.label :patient_id, style: "display: block" %>
    <%= form.text_field :patient_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
