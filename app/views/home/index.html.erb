<% if user_signed_in? %>
  <div> Greetings, <%= current_user.email %></div>
  <%= button_to "Sign Out", destroy_user_session_path, method: :delete %>
  <% else %>
  <%= button_to "Sign In", new_user_session_path %>
<% end %>
<h1> Welcome to our Hospital! </h1>
<img src=/img/cute-photos-of-cats.jpg>